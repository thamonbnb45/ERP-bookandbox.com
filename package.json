{
  "name": "bookandbox-erp-root",
  "version": "1.0.0",
  "scripts": {
    "build": "cd bookandbox-hub && npm install && npm run build && cd .. && rm -rf backend/public && cp -r bookandbox-hub/out backend/public && cd backend && npm install",
    "start": "cd backend && node index.js"
  }
}
